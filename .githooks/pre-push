#!/usr/bin/env bash
# Local twin of the CI leak-scan gate. Enable once per clone:
#   git config core.hooksPath .githooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
bash scripts/leak-scan.sh
